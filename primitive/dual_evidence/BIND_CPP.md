# FULL Bind — Python Spike Agents ↔ C++ Residual Core

**Status:** scaffold GREEN · bind in progress  
**Squad:** THE BEASTIE BOYZ / Blam  
**EXTERNAL-clean.**

---

## Goal

Python spike agents call the **same** dual-evidence residual policy as the high-performance C++ path — one second solid, two surfaces.

---

## Layers

| Layer | Artifact | Role |
|-------|----------|------|
| Primitive | `dual_evidence.hpp` / `.py` | σ_Δ + second_solid + dual_confirm |
| Agent gate | `spike_agent_gate.hpp` | classical_choose / dual_choose |
| Sort residual | `residual/secondary_parity.hpp` | production SP residual |
| Python surface | `demo_spike_agent.py` | death-match + chaos |
| Bind | this doc | FULL bind target |

---

## Bind plan

### B0 — scaffold (shipped)
- [x] `spike_agent_gate.hpp`
- [x] Chaos non-stationary stress GREEN
- [x] Shared `dual_evidence.hpp`

### B1 — C API export
```c
float de_sigma_amps(const float* amps, int n);
int   de_dual_choose(float L_rate, float L_sigma, float R_rate, float R_sigma, float floor);
```
→ `libdual_evidence.so`

### B2 — ctypes from Python
Replace pure-Python `dual_choose` with lib call; bit-identical death-match.

### B3 — residual core optional path
FULL residual → optional PhotonicSort talent menu under `-DSECONDARY_PARITY`.

### B4 — pybind11 module
`dual_evidence_cpp` installable module.

---

## Non-claims

Bind does not change Field-Level sorter claim. Agent suites remain synthetic.

**One second solid. Full bind next.**
