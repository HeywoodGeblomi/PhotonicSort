# Non-Claims — PhotonicSort

Applies to main after hybrid residual path-(a) land (FIELD_LEVEL_CLAIM_v0.2) and Field-Level Claim v0.5.

## Claims (current)

### Hybrid residual path-(a) configuration (`residual/hybrid_residual_menu.hpp`)

* Dual residual / Secondary Parity talent drive is the **DEFAULT production path** (PR #167).
* Expanded Field Suite unscoped soft_count = 0 at soft_threshold **1.20** on measured x86_64 + aarch64 (i32/u32/i64).
* Primary geo ≤ 0.90× vs best specialized on measured surface.
* EXTERNAL-clean visible-metric routing; worst-case O(n log n).
* Escape hatch only: `-DCLASSICAL_RESIDUAL` restores classical unconditional ska on mid-band HE.

### Pure residual alternate surface

* Pure residual menu remains available as a separate engineering surface.
* Pure residual alone does **not** claim unscoped soft=0 on residual-quality / HE territory.

## Hybrid routing (honest)

| Zone | Residual |
|------|----------|
| STRUCTURE | pure O(n) |
| equal_heavy | library-strength pdq |
| few-unique / sawtooth / reverse-seg | pure residual |
| HE / gaussianish | library ska (talent-gated on borderline) |
| mixed_blocks / residual-quality default | library ska/pdq |

## Non-claims

1. **Not a pure residual excellence claim** for path-(a) unscoped soft=0.
2. **Not a universal replacement** for library pdq/ska outside the measured suite.
3. **Not a novel adaptive complexity theorem** (path (b) not claimed).
4. Formal scope of pure residual path limits remains documented for the pure residual surface only.
5. Soft-gate is charged soft@1.20(vs pdq)=0 on the locked charged surface only.

Always surface hybrid vs pure residual distinction when summarizing. Honesty primary.

## Packaging

6. **Packaging does not expand claims.** A clean CMake / pip / cargo / Docker surface does not imply universal superiority outside the measured charged surface.
7. **Default production path** is dual residual (Secondary Parity talent drive) after PR #167. `-DCLASSICAL_RESIDUAL` is the only compile-time escape hatch. Soft-gate remains charged soft@1.20(vs pdq)=0 on both ISAs.
8. Strangers receive the same EXTERNAL-clean, honesty-gated package the money-shot validates.

THE BEASTIE BOYZ · 2026-08-15
