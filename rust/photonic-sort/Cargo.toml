[package]
name = "photonic-sort"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Safe Rust API for PhotonicSort — adaptive hybrid sort (C11 core v1.3.1-c)"
readme = "README.md"
keywords = ["sort", "adaptive", "hybrid", "photonic", "radix"]
categories = ["algorithms", "api-bindings"]
include = [
    "src/**",
    "README.md",
    "LICENSE",
]

[dependencies]
photonic-sort-sys = { workspace = true }
