# Path (b) Lateral Map — Candidate Only

**Date:** 2026-08-12  
**Rule:** No theorem claimed. Map only. EXTERNAL-clean.  
**Frame:** Ax+By=C — literal surface × lateral measure → candidate theory.

---

## One line

Hybrid residual is a **visible-metric adaptive choice**: sample ≈ classical presortedness → residual class.

---

## Classical anchors → our probes

| Classical | Probe (hybrid menu) | Residual bias |
|-----------|---------------------|---------------|
| **Inv** (inversions) | `inv`, `dense_inv` | high → HE/ska or pdq |
| **Runs** | `runs`, `desc_runs` | reverse-seg → pure; mid runs → pdq |
| **Rem / almost-sorted** | low `inv` + consecutive `dom` | pure O(n) / low_disorder |
| **Card / few unique** | sample `u`, `eq` | counting / few_wide pure |
| **α-near position** | consecutive domain + local shuffle | mixed_blocks → ska/pdq |
| **LRM / monotone runs** | `desc_runs` + inv band | pure reverse_runs |

LRM ≈ left-to-right extrema; our `desc_runs` is the descending sample analog.

α-sortedness (near-correct position): consecutive `dom≈n` + sparse local disorder ≈ mixed_blocks shape.

---

## T1 candidate (not claimed)

```
residual = f(Înv, Runŝ, û, êq, dôm)
```

`f` = hybrid dispatch table. Cost = probe O(S) + residual T(n).  
Misrouting = `f(sample) ≠ f(full)` — T3 territory.

---

## T2 candidate (not claimed)

If sample (Inv, Runs) concentrates around true (Inv, Runs), hybrid residual is an **approximate adaptive sort** in the classical sense (Mannila / Estivill-Castro–Petersson measures), without new asymptotics yet.

---

## Paradox labor (multi-arch)

Theory needs the **same** hybrid surface on both ISAs.  
Minimal labor: `expanded-field.yml` builds hybrid with ska in `baselines/` (already fetched). Soft@1.20 is the gate.

---

## Kill

No concrete bound or measure in fixed window → park (PATH_B_TABLE_SET).

**THE BEASTIE BOYZ**
